agent {
  features {
    parameterExtractionRules = [{
      loggingClass = "core.ThreadHandler"
      lineNumber = 10
      regex = "^Starting ([a-z0-9_]+) with ([0-9]+) threads$"
      keys = ["containerId", "numberOfThreads"]
    }]
  }
}